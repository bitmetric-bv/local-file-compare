# local-file-compare
Client-side file comparison tool — load two CSV or XLSX files, pick a key column, and instantly see row-level differences highlighted in your browser. No server, no data upload, fully private.
